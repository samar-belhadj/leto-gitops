apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: leto-latest-ingress
spec:
  rules:
    - host: samar.flask-app.ht.nearteam.fr
      http:
        paths:
          - backend:
              serviceName: leto-latest-svc
              servicePort: 8080
    
